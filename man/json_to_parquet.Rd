% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json_to_parquet.R
\name{json_to_parquet}
\alias{json_to_parquet}
\title{Convert an OpenAlex JSON file to Parquet with abstract and citation}
\usage{
json_to_parquet(
  input_json_file,
  output_parquet_file,
  temp_jsonl = tempfile(fileext = ".jsonl"),
  jq_path = "jq",
  page = NUL
)
}
\arguments{
\item{input_json_file}{Path to the OpenAlex JSON file}

\item{output_parquet_file}{Path to the output Parquet file}

\item{temp_jsonl}{Optional path for the temporary intermediate JSONL file (default: \code{tempfile()}) containing
the \code{abstract} as well as the \code{citation} generated by \code{jq_execute()}}

\item{jq_path}{Path to the jq executable (default: "jq")}
}
\value{
Invisibly returns the path to the output Parquet file
}
\description{
This function combines \code{jq_execute()} and the Arrow package to convert an OpenAlex-style
JSON file containing a \code{results} array into a Parquet file. It reconstructs the abstract
and generates a citation for each entry, saving the result without loading full data into R.
}
