% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pro_request_parquet.R
\name{pro_request_parquet}
\alias{pro_request_parquet}
\title{Convert JSON files from pro_request() directly to Apache Parquet}
\usage{
pro_request_parquet(
  input_json = NULL,
  output = NULL,
  add_columns = list(),
  overwrite = FALSE,
  verbose = TRUE,
  progress = TRUE,
  delete_input = FALSE,
  sample_size = 1000,
  workers = NULL,
  enrich = TRUE,
  schema = "auto"
)
}
\arguments{
\item{input_json}{Directory of JSON files returned by \code{\link[=pro_request]{pro_request()}}.}

\item{output}{Output directory for the Parquet dataset.}

\item{add_columns}{Named list of scalar constant columns to embed in every
output record (e.g. \code{list(query = "my_filter")}).  Values are embedded as
SQL string literals; only character scalars are supported.}

\item{overwrite}{Logical.  Overwrite \code{output} if it already exists.
Default \code{FALSE}.}

\item{verbose}{Logical.  Show progress messages.  Default \code{TRUE}.}

\item{progress}{Logical.  Show a progress bar.  Default \code{TRUE}.}

\item{delete_input}{Logical.  Delete \code{input_json} after a successful
conversion.  Default \code{FALSE}.}

\item{sample_size}{Integer.  Number of records per file passed to DuckDB's
\code{sample_size} option during schema inference.  Use \code{-1} to read all
records (accurate but slow for large files).  Default \code{1000}.}

\item{workers}{Integer.  Number of parallel workers.
\code{NULL} or \code{1} runs sequentially.  Default \code{NULL}.}

\item{enrich}{Logical.  When \code{TRUE} (the default) and the inferred schema
contains \code{abstract_inverted_index} / \code{authorships} / \code{publication_year},
add \code{abstract} and \code{citation} computed columns.}

\item{schema}{Controls use of a pre-built baseline schema for type
resolution.  Possible values:
\describe{
\item{\code{"auto"} (default)}{Auto-detect the OpenAlex entity type from the
inferred columns, then load the matching schema from the user cache
(populated by \code{\link{oa_schema}(update = TRUE)}) or the schemas
bundled with the package.  For each column where DuckDB runtime
inference produced the ambiguous \code{JSON} fallback type, the baseline
type is used instead.  Falls back silently to runtime-only inference
when the entity cannot be detected or no schema is found.}
\item{\code{"none"} or \code{NULL}}{Skip the baseline entirely; behaviour is
identical to package versions before this feature was added.}
\item{A file path}{Path to a CSV with columns \code{col_name} / \code{col_type}.
Used directly as the baseline.}
\item{A directory path}{Auto-detect entity, then look for
\verb{<entity>.csv} inside that directory.}
}}
}
\value{
Output directory path (invisibly).
}
\description{
Single-step replacement for the two-step
\code{pro_request_jsonl()} + \code{pro_request_jsonl_parquet()} pipeline.
Reads the JSON files written by \code{\link[=pro_request]{pro_request()}} and converts each one to a
Parquet file using DuckDB, with no intermediate JSONL on disk.
}
\details{
For works entities the function detects the presence of
\code{abstract_inverted_index}, \code{authorships}, and \code{publication_year} in the
inferred schema and, when \code{enrich = TRUE} (the default), adds two computed
columns:
\itemize{
\item \strong{\code{abstract}} — plain text reconstructed from \code{abstract_inverted_index}.
\item \strong{\code{citation}} — \code{"Author (year)"} / \code{"A & B (year)"} / \code{"A et al. (year)"}.
}
}
\section{File format}{

\code{\link[=pro_request]{pro_request()}} writes one JSON file per API page.  For paginated queries
each file has the structure \verb{\{"results": [...], "meta": \{...\}\}}.  For
group-by queries the array field is \code{"group_by"}.  For single-record lookups
the file is a bare JSON object.  All three formats are handled automatically.
}

\section{Output layout}{

The subdirectory structure of \code{input_json} is preserved, with hive-partition
naming (\verb{query=<name>/}, \verb{query_l2=<name>/}, …) so that Arrow/DuckDB can
read the result as a partitioned dataset.  A \code{page} column is added to each
record with a value derived from the source filename (or subdirectory for
multi-query inputs).
}

\seealso{
\code{\link[=pro_request]{pro_request()}} to download the JSON files,
\code{\link[=pro_request_jsonl]{pro_request_jsonl()}} and \code{\link[=pro_request_jsonl_parquet]{pro_request_jsonl_parquet()}} for the older
two-step pipeline (now deprecated).
}
